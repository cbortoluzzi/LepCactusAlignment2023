Download Ensembl annotation

The python script takes three inputs: 
- a tsv file with assembly information (i.e. vertebrata_202111.tsv);
- a tsv file downloaded from Ensembl Rapid release (ftp://ftp.ensembl.org/pub/rapid-release/species_metadata.json) (i.e. species_metadata_202112.json).
- a tsv file with information on the path of
